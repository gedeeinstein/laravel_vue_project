@if( !empty( $left ))
    <div class="px-1 col-sm-5 pt-2 pt-sm-0">
        {{ $left }}
    </div>
@endif

@if( !empty( $right ))
    <div class="px-1 col-sm-7 pt-2 pt-sm-0">
        {{ $right }}
    </div>
@endif